spring:
  application:
    name: admin-server
  cloud:
    config:
      label: registry-first
      discovery:
        enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
