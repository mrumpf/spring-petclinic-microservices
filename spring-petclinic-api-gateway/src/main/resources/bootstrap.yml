spring:
  application:
    name: api-gateway
  cloud:
    config:
      label: registry-first
      discovery:
        enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
